# Polish 5 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Deliver the eight TASK-POLISH5 copy, navigation, colophon, product-card, and wordmark changes on `tri/30-polish5` without changing section reachability or adding dependencies.

**Architecture:** Keep profile content in `content/profile.json`, keep all-section navigation in the palette and line-nav, and limit the four-link array only to `SiteNav`. Render the footer from static profile data plus build constants injected by `next.config.mjs`, and adapt the staged registry glow component into a generic client boundary whose product children remain server-rendered.

**Tech Stack:** Next.js 16 static export, React 19, TypeScript strict, Tailwind CSS 4, Vitest, Node test runner, GitHub Actions, Cloudflare static assets.

## Global Constraints

- Navbar links are exactly Work, Products, Timeline, Contact in that order on desktop and mobile.
- The command palette and line-nav retain every section and the existing section ids and order.
- The identity tagline is exactly `Using AI & automation to solve security issues` and remains static.
- The `I started coding at 14.` profile field and render slot are removed.
- Footer facts are limited to the seven approved cells and use only real repository or profile data.
- Footer copy contains no `Cloudflare Workers`, registry credit, chanhdai branding, DMCA badge, or inspired-by list.
- The navbar wordmark is exactly `Zuriel Shanley`, uses `ShimmeringText`, stays static under reduced motion, and has a token-based AA base color in both themes.
- The staged glow grid SHA is `697511424edc76ff595359fdf5a32641096b62a102ca54d3fdac6d5d4954ad73`.
- No new npm dependency and no em dash in copy or docs.

---

### Task 1: Identity copy and schema

**Files:**
- Modify: `content/profile.test.ts`
- Modify: `lib/identity-header.test.ts`
- Modify: `scripts/generate-dossier.test.mjs`
- Modify: `content/profile.json`
- Modify: `content/schema.ts`
- Modify: `components/sections/identity-header.tsx`
- Modify: `scripts/generate-dossier.mjs`
- Regenerate: `content/chat-projection.md`

**Interfaces:**
- Consumes: the strict `ProfileSchema` and `IdentityHeader({ profile }: { profile: Profile })`.
- Produces: an identity with `tagline` but no `bio_hook`, with generated chat projection content derived from the remaining allowlisted fields.

- [ ] **Step 1: Write failing identity tests**

  Assert the exact new tagline, assert `profile.identity` has no `bio_hook`, assert identity markup omits the removed sentence and its former `dossier-body` paragraph, and remove `one_liner` from the expected generated public profile.

- [ ] **Step 2: Run the focused tests and verify RED**

  Run: `npx vitest run content/profile.test.ts lib/identity-header.test.ts && node --test scripts/generate-dossier.test.mjs`

  Expected: failures identify the old tagline, existing `bio_hook`, and rendered sentence.

- [ ] **Step 3: Implement the minimal identity changes**

  Replace the tagline, delete `bio_hook` from JSON and Zod schema, remove its paragraph from `IdentityHeader`, and remove the generated `one_liner` projection.

- [ ] **Step 4: Regenerate and verify GREEN**

  Run: `node scripts/generate-dossier.mjs && npx vitest run content/profile.test.ts lib/identity-header.test.ts && node --test scripts/generate-dossier.test.mjs`

  Expected: all focused identity and projection tests pass and `content/chat-projection.md` contains neither removed string.

### Task 2: Navbar decongestion and shimmering wordmark

**Files:**
- Modify: `lib/core-sections.test.ts`
- Modify: `lib/registry-identity-motion.test.tsx`
- Modify: `lib/terminal-focus.test.ts`
- Modify: `scripts/light-theme-contrast.test.mjs`
- Modify: `components/site-nav.tsx`

**Interfaces:**
- Consumes: `ShimmeringText`, the existing delegated terminal opener, haptic bridge, theme switcher, and command palette opener.
- Produces: one exported `SITE_NAV_LINKS` tuple containing Work, Products, Timeline, Contact for both desktop and mobile render paths.

- [ ] **Step 1: Write failing navbar tests**

  Assert two copies of each approved href, no copies of other section hrefs inside `SiteNav`, four mobile links, `aria-label="Zuriel Shanley"`, one shimmering visible label, no ZST text, and both light and dark `--text-3` contrast of at least 4.5:1 against `--canvas`.

- [ ] **Step 2: Run the focused tests and verify RED**

  Run: `npx vitest run lib/core-sections.test.ts lib/registry-identity-motion.test.tsx lib/terminal-focus.test.ts && node --test scripts/light-theme-contrast.test.mjs`

  Expected: the old ten-link array and ZST spotlight assertions fail.

- [ ] **Step 3: Implement the minimal navbar changes**

  Replace the link tuple with the approved order, render `ShimmeringText` inside the unchanged terminal button, use the matching accessible name, preserve data attributes and haptics, and constrain the visible wordmark with no-wrap mobile-safe classes.

- [ ] **Step 4: Run the focused tests and verify GREEN**

  Repeat the Step 2 command and require zero failures.

### Task 3: Build-time colophon footer

**Files:**
- Modify: `lib/capstone-components.test.ts`
- Modify: `scripts/verify-build.test.mjs`
- Modify: `components/footer.tsx`
- Modify: `lib/build-info.ts`
- Modify: `next.config.mjs`
- Modify: `.github/workflows/deploy.yml`
- Modify: `app/page.tsx`

**Interfaces:**
- Consumes: `BUILD_DATE`, `BUILD_SHA`, package versions from `package.json`, and the existing profile social array.
- Produces: `Footer({ buildDate?, buildSha?, socials })`, a seven-cell responsive colophon, and the local fallback literal `dev`.

- [ ] **Step 1: Write failing colophon tests**

  Render the footer with a full SHA and fixed date. Assert the title and descriptor, the exact seven labels, `@Leiruz`, a seven-character linked build, date, source, package-derived next/react/tailwindcss values, Geist, Cloudflare Web Analytics, ZST, two labelled icon links, visible focus classes, and absence of every prohibited footer string. Change build-resolution tests to require `dev` after empty environment sources.

- [ ] **Step 2: Run the focused tests and verify RED**

  Run: `npx vitest run lib/capstone-components.test.ts && node --test scripts/verify-build.test.mjs scripts/workflow-config.test.mjs`

  Expected: the old document-control footer and Git fallback assertions fail.

- [ ] **Step 3: Implement the build constants and colophon**

  Resolve deployment SHA from injected environment values with local `dev` fallback, keep one build-date constant, inject `BUILD_SHA` explicitly during the deploy build, derive displayed stack versions from `package.json`, render bordered responsive cells, and pass the profile socials from `app/page.tsx`.

- [ ] **Step 4: Run the focused tests and verify GREEN**

  Repeat the Step 2 command and require zero failures.

### Task 4: Vendored glow grid and Products adoption

**Files:**
- Create through extractor: `components/registry/glow-card-grid.tsx`
- Modify: `lib/registry-final-sections.test.tsx`
- Modify: `lib/final-sections.test.ts`
- Modify: `components/sections/products.tsx`
- Modify: `styles/globals.css`
- Modify: `docs/components-map.md`

**Interfaces:**
- Consumes: `GlowCardGrid` and `GlowCard` wrappers, `ScrollFadeEffect`, and existing `Product` content and media filtering.
- Produces: generic product card wrappers with `data-slot="glow-card-grid"`, `data-slot="glow-card"`, keyboard focus, token-based hover glow, and pointer tracking disabled when reduced motion is preferred.

- [ ] **Step 1: Write failing glow adoption tests**

  Assert the registry exports exist, every product is inside a glow card with a tab stop, the grid slot is present, all profile content remains rendered, CSS supplies hover and focus glow plus reduced-motion overrides, the pointer-enablement helper returns false for reduced motion, and the docs adoption note includes the recorded SHA.

- [ ] **Step 2: Run the focused tests and verify RED**

  Run: `npx vitest run lib/final-sections.test.ts lib/registry-final-sections.test.tsx`

  Expected: the missing vendored module or missing glow slots causes the intended failure.

- [ ] **Step 3: Extract and adapt the staged component**

  Run: `node extract-registry.mjs registry-stage . glow-card-grid`

  Adapt the extracted file without removing its provenance header. Scope pointer movement to the grid, skip listener setup when the reduced-motion media query matches, expose generic card children, and keep the component dependency-free.

- [ ] **Step 4: Adopt the grid and style the cards**

  Compose the existing Product card body inside `GlowCard`, add token-based radial border and surface glow styles with keyboard focus visibility, add a static reduced-motion fallback, and add the adoption note with the staged SHA.

- [ ] **Step 5: Run the focused tests and verify GREEN**

  Repeat the Step 2 command and require zero failures.

### Task 5: Release verification and commits

**Files:**
- Verify: all changed tracked files
- Verify generated export: `out/index.html`

**Interfaces:**
- Consumes: all four completed slices.
- Produces: a verified `tri/30-polish5` branch with conventional commits.

- [ ] **Step 1: Run complete gates**

  Run `npx vitest run`, `npm run build`, `node scripts/perf-gate.mjs`, and `npm run layout:gate`, each as a fresh command with exit code 0.

- [ ] **Step 2: Inspect exported HTML**

  Scope checks to the navbar, identity, Products, and footer regions in `out/index.html`. Require four navbar labels in both render paths, the new wordmark and tagline, no removed bio sentence, glow-card product markup, the colophon title, and no prohibited footer copy.

- [ ] **Step 3: Review repository state**

  Run `git diff --check`, review `git diff --stat` and the complete diff, confirm `package.json` and `package-lock.json` have no dependency changes, and preserve all pre-existing untracked files.

- [ ] **Step 4: Commit conventional changes**

  Commit coherent slices with conventional messages, then confirm `git status --short --branch` shows only the known pre-existing untracked artifacts.
