# Polish 7 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remove the Timeline and line-nav collision, make the navbar wordmark static, add the public Instagram profile to footer socials, and compact the animated theme toggle.

**Architecture:** Keep the profile as the social-link source of truth and retain the existing server-rendered component boundaries. Put the complete experience-01 block inside the shared dossier shell so its two-column layout receives the same desktop gutter contract as neighboring sections. Restrict navbar changes to presentation and sizing so the terminal trigger, icon swap, sound, and circle-blur transition remain unchanged.

**Tech Stack:** Next.js 16 static export, React 19, TypeScript strict, Tailwind CSS 4, Vitest, Node test runner, Puppeteer Core, Chromium, Lighthouse, and Cloudflare Worker static assets.

## Global Constraints

- Work only on `tri/36-polish7`; never switch branches.
- No em dash appears in copy or docs.
- Line-nav items, palette items, palette, and section order remain unchanged.
- The Timeline keeps the experience-01 internal two-column layout.
- Mobile and tablet Timeline layout remains unchanged.
- The navbar wordmark keeps its accessible name and terminal-trigger behavior.
- Roles text-flip shimmer and slide-to-unlock shimmer remain unchanged.
- The theme icon swap, sound, and circle-blur transition remain unchanged.
- The 375px navbar has no horizontal overflow.
- No new dependency is added.
- Lighthouse performance remains at least 0.90.

---

### Task 1: Timeline rail regression

**Files:**
- Modify: `scripts/layout-gate.mjs`
- Modify: `components/sections/timeline.tsx`
- Modify: `lib/core-sections.test.ts`

**Interfaces:**
- Consumes: `[data-section-line-nav='true']`, `#timeline-title`, the first `[data-work-organization='true']`, the first `[data-work-position='true']`, and `.dossier-page .dossier-shell` geometry.
- Produces: a Timeline whose complete experience-01 block uses the shared shell gutter, plus a Chromium assertion that each representative Timeline box has zero rectangle intersection with the fixed rail.

- [ ] **Step 1: Capture the unmodified Chromium geometry**

  Build the current branch, then use the existing `chrome-launcher`, `puppeteer-core`, and static-server utilities to center each Timeline target at 1280, 1440, 1920, and 2560 pixels. Record target left edge, rail right edge, and intersection width.

- [ ] **Step 2: Add the failing layout assertion**

  Extend `measureViewport` to scroll and measure the heading, first organization, and first role card against the fixed rail. Assert rectangle non-intersection for every desktop measurement.

- [ ] **Step 3: Verify RED on the pre-fix export**

  Run: `npm run layout:gate`

  Expected: FAIL at 1280px because the Timeline heading and first organization begin beneath the rail.

- [ ] **Step 4: Add the shell contract test**

  Require `data-slot="experience-01"` to be nested inside a `dossier-shell` wrapper in the Timeline markup.

- [ ] **Step 5: Verify the focused component test is RED**

  Run: `npx vitest run lib/core-sections.test.ts`

  Expected: FAIL because the current Timeline section has no dossier shell.

- [ ] **Step 6: Implement the minimal shell fix**

  Wrap the existing `Experience01` call with `<div className="dossier-shell min-w-0">`. Do not modify the experience adapter, organization grid, or mobile breakpoints.

- [ ] **Step 7: Rebuild and verify GREEN**

  Run: `npx vitest run lib/core-sections.test.ts && npm run build && npm run layout:gate`

  Expected: PASS with zero Timeline intersections at all measured widths.

### Task 2: Static navbar wordmark

**Files:**
- Modify: `components/site-nav.tsx`
- Modify: `lib/registry-identity-motion.test.tsx`
- Modify: `lib/terminal-focus.test.ts`

**Interfaces:**
- Consumes: the existing terminal-trigger button and the shimmer base token `var(--text-3)`.
- Produces: a plain `span` reading `Zuriel Shanley` with the same type scale, weight, tracking, accessible button name, and click behavior.

- [ ] **Step 1: Update tests to require a static wordmark**

  Assert the SiteNav markup contains a dedicated static wordmark marker and contains no `data-slot="shimmering-text"` inside the brand button.

- [ ] **Step 2: Verify RED**

  Run: `npx vitest run lib/registry-identity-motion.test.tsx lib/terminal-focus.test.ts`

  Expected: FAIL because SiteNav still renders `ShimmeringText`.

- [ ] **Step 3: Replace only the navbar wrapper**

  Remove the SiteNav shimmer import and render the current text classes on a static `span` using `text-text-3`. Leave the identity roles and intro gate components untouched.

- [ ] **Step 4: Verify GREEN**

  Repeat Step 2 and require zero failures.

### Task 3: Instagram footer social

**Files:**
- Modify: `content/profile.json`
- Modify: `content/schema.ts`
- Modify: `components/footer.tsx`
- Modify: `lib/capstone-components.test.ts`
- Modify: `content/profile.test.ts`

**Interfaces:**
- Consumes: `profile.identity.socials` and the existing footer social mapping.
- Produces: an Instagram social with source URL `https://www.instagram.com/zureal.st`, accessible name `Instagram`, `target="_blank"`, `rel="me noopener"`, tracked outbound rendering, and a monochrome inline SVG.

- [ ] **Step 1: Write failing schema and footer tests**

  Extend the expected social union and count, require the exact Instagram source URL, require three footer social links, and require the Instagram link and SVG contract.

- [ ] **Step 2: Verify RED**

  Run: `npx vitest run content/profile.test.ts lib/capstone-components.test.ts lib/identity-header.test.ts`

  Expected: FAIL because Instagram is absent and the social schema accepts only two platforms.

- [ ] **Step 3: Add the social source and glyph**

  Add Instagram to `profile.json`, expand `SocialSchema` to three platforms and three entries, set footer profile links to `rel="me noopener"`, and add an outline camera glyph that inherits `currentColor`.

- [ ] **Step 4: Verify GREEN**

  Repeat Step 2 and require zero failures.

### Task 4: Compact theme toggle

**Files:**
- Modify: `styles/globals.css`
- Modify: `components/registry/theme-switcher.tsx`
- Modify: `components/registry/client-enhancements.tsx`
- Modify: `components/registry/theme-switcher.test.tsx`

**Interfaces:**
- Consumes: the existing ThemeSwitcherControl, IconSwap, document sprite, sound scheduler, and circle-blur view transition.
- Produces: a 2.25rem square control with a 1.5rem icon and a matching server placeholder, while retaining the existing focus ring and theme-selection flow.

- [ ] **Step 1: Change the size contract test**

  Require `.theme-switcher-button` width and height to be `2.25rem`, its SVG to be `1.5rem`, and both unresolved loading placeholders to use the same 2.25rem footprint.

- [ ] **Step 2: Verify RED**

  Run: `npx vitest run components/registry/theme-switcher.test.tsx`

  Expected: FAIL against the current 2.75rem control and 1rem icon.

- [ ] **Step 3: Implement the compact dimensions**

  Change only the control dimensions, icon dimensions, and placeholder footprint. Preserve all animation, audio, transition, label, and focus selectors.

- [ ] **Step 4: Verify GREEN**

  Repeat Step 2 and require zero failures.

### Task 5: Responsive inspection, full gates, and commits

**Files:**
- Verify: all changed tracked files
- Verify generated export: `out/index.html`

**Interfaces:**
- Consumes: all four completed polish changes.
- Produces: fresh responsive evidence, a reviewed branch diff, and conventional commits on `tri/36-polish7`.

- [ ] **Step 1: Inspect Chromium geometry**

  Rebuild and capture Timeline rail intersections at 1280, 1440, 1920, and 2560 pixels. Inspect both themes and the 375px navbar for overflow, focus visibility, theme-control dimensions, and preserved icon-swap markup.

- [ ] **Step 2: Run every required gate fresh**

  Run `npx vitest run`, `npm test`, `npm run build`, `node scripts/perf-gate.mjs`, and `npm run layout:gate` as separate commands. Require exit code 0 and Lighthouse at least 0.90.

- [ ] **Step 3: Review repository state**

  Run `git diff --check`, inspect the complete tracked diff, confirm there are no em dashes in changed copy or docs, and preserve all pre-existing untracked artifacts.

- [ ] **Step 4: Create conventional commits**

  Commit coherent slices on `tri/36-polish7`. The Timeline commit body must state that the new gate failed on the pre-fix export and identify the failing viewport widths.
