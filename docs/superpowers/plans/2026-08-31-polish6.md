# Polish 6 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Integrate the four TASK-POLISH6 registry blocks on `tri/31-polish6` while preserving real-data, accessibility, static-export, layout, and landing-performance contracts.

**Architecture:** Keep every section server-rendered from committed profile and analytics data. Add client code only at isolated enhancement boundaries: p5 behind the 404 route and reduced-motion check, and the licensed Bklit chart behind the Insights renderer boundary. Adapt each registry block to the dossier token system while preserving its recognizable structural layout and recording exact source provenance.

**Tech Stack:** Next.js 16 static export, React 19, TypeScript strict, Tailwind CSS 4, p5 1.9, Bklit line chart, Vitest, Node test runner, Lighthouse, and Cloudflare Worker static assets.

## Global Constraints

- Section order and ids are unchanged; line-nav and palette entries remain unchanged.
- `#insights` and `#timeline` keep their current positions and navigation entries.
- Render only real `profile.json`, `testimonials.json`, and committed Cloudflare analytics snapshot data.
- No demo metrics, brands, quotes, artwork, sounds, or remote assets ship in runtime code.
- No chanhdai branding ships in vendored runtime or exported HTML.
- No em dash appears in copy or docs.
- Colors use existing dossier tokens in both themes.
- Reduced-motion sessions receive static, fully readable content and no optional 404 game runtime.
- The landing route does not load p5, and the landing performance and layout gates remain green with Lighthouse at least 0.90.
- New lockfile dependency resolution uses only `npx npm@10.9.0 install --package-lock-only`.

---

### Task 1: Registry provenance and ZST 404

**Files:**
- Create: `registry-stage/not-found-01.json`
- Create: `components/registry/not-found-game-loader.tsx`
- Create: `components/registry/not-found-game-canvas.tsx`
- Modify: `app/not-found.tsx`
- Modify: `package.json`
- Regenerate: `package-lock.json`
- Modify: `styles/globals.css`
- Modify: `docs/components-map.md`
- Modify: `lib/capstone-components.test.ts`
- Modify: `lib/dossier-visual-direction.test.ts`
- Modify: `scripts/perf-gate.mjs`
- Modify: `scripts/perf-gate.test.mjs`
- Modify: `scripts/verify-build.mjs`
- Modify: `scripts/verify-build.test.mjs`

**Interfaces:**
- Consumes: the live `not-found-01` manifest, existing dossier tokens, and Cloudflare `not_found_handling: "404-page"`.
- Produces: an exported `404.html` with a static ZST mark, readable 404 copy, a home link, and an optional keyboard-operable p5 game whose module is requested only on the 404 route and never under reduced motion.

- [ ] **Step 1: Stage the exact registry source after recording its byte evidence**

  Fetch `https://chanhdai.com/r/not-found-01.json` to `registry-stage/not-found-01.json`, require SHA256 `fba756a446d848845eba5b2edd12d07b1697f27dcf7fa2b71bd4dd9dcc4a4f34`, and record the URL, byte count, and SHA in `docs/components-map.md`.

- [ ] **Step 2: Write failing 404 tests**

  Assert static markup includes ZST, missing-page text, and `href="/"`; runtime source and exported HTML exclude chanhdai names, URLs, artwork, and sound; reduced motion blocks the game import; keyboard controls are scoped to the game; the landing build does not reference p5; and the local static server returns `404.html` for a bogus document path while missing assets remain plain 404 responses.

- [ ] **Step 3: Run focused tests and verify RED**

  Run: `npx vitest run lib/capstone-components.test.ts lib/dossier-visual-direction.test.ts && node --test scripts/perf-gate.test.mjs scripts/verify-build.test.mjs`

  Expected: failures identify the missing ZST block, loader/game contracts, and static-server fallback.

- [ ] **Step 4: Implement the static shell and route-only game**

  Adapt the registry block to p5 draw primitives only, with a ZST brick pattern, no audio, no remote images, a real start/restart button, focus-scoped arrow and Space handling, and an `aria-live` status. Keep the base 404 message and home link visible without JavaScript. Check reduced motion before dynamically importing the game module.

- [ ] **Step 5: Add dependencies using the prescribed lockfile command**

  Add `p5` and its matching type package to `package.json`, then run exactly `npx npm@10.9.0 install --package-lock-only` to regenerate the lockfile.

- [ ] **Step 6: Run the focused tests and verify GREEN**

  Repeat Step 3 and require zero failures.

### Task 2: Social proof adoption in Worked with

**Files:**
- Create: `components/registry/social-proof-01.tsx`
- Modify: `components/sections/brands-wall.tsx`
- Modify: `styles/globals.css`
- Modify: `docs/components-map.md`
- Modify: `lib/registry-final-sections.test.tsx`
- Modify: `lib/registry-data-adapters.test.tsx`

**Interfaces:**
- Consumes: `profile.stack_brands.brands`, `profile.stack_brands.disclaimer`, the existing `BrandIcon`, and logos-carousel enhancement.
- Produces: a social-proof-01 structural adapter inside `#brands`, headed only by the existing Worked with language, with ten real brand names, glyphs, contexts, and the non-endorsement disclaimer.

- [ ] **Step 1: Write failing Brands adoption tests**

  Require a `social-proof-01` block marker inside `#brands`, all ten real brands and contexts exactly once, the disclaimer, responsive semantic markup, and no staged sample company names or endorsement language.

- [ ] **Step 2: Run focused tests and verify RED**

  Run: `npx vitest run lib/registry-final-sections.test.tsx lib/registry-data-adapters.test.tsx`

  Expected: the missing block marker and layout fail while current real-data assertions remain valid.

- [ ] **Step 3: Adapt and integrate the block**

  Preserve social-proof-01's bordered grid and responsive logo presentation, but accept profile brand children instead of its demo array. Reuse the existing carousel behavior and reduced-motion static grid. Keep the `#brands` id, Fig. 3 heading, section order, contexts, and disclaimer unchanged.

- [ ] **Step 4: Record the placement decision and verify GREEN**

  Record in `docs/components-map.md` that this logo-forward block belongs in Worked with, then repeat Step 2 and require zero failures.

### Task 3: Licensed Bklit chart and metrics-01 Insights layout

**Files:**
- Create: `registry-stage/bklit-line-chart.json`
- Create: `components/registry/bklit/` chart files required by the exact registry payload
- Create: `components/registry/metrics-01.tsx`
- Modify: `components/sections/visitor-insights.tsx`
- Modify: `package.json`
- Regenerate: `package-lock.json`
- Modify: `styles/globals.css`
- Modify: `docs/components-map.md`
- Modify: `lib/visitor-insights.test.ts`
- Modify: `scripts/verify-build.mjs`
- Modify: `scripts/verify-build.test.mjs`

**Interfaces:**
- Consumes: `AnalyticsSnapshot`, `summarizeAnalytics`, `buildAnalyticsChart`, `formatAnalyticsDate`, and the MIT-licensed `https://ui.bklit.com/r/line-chart.json` payload.
- Produces: the metrics-01 header, divider, three-card metric grid, and Bklit-based visits/views visualization using only 40 visits, 60 views, and the 2026-08-30 busiest day from the current committed snapshot.

- [ ] **Step 1: Preserve license evidence before source extraction**

  Record that `packages/ui` and the shadcn registry are MIT under `https://raw.githubusercontent.com/bklit/bklit-ui/main/LICENSE`, preserve the copyright and permission notice with the vendored source, explicitly exclude proprietary Studio code, fetch the exact line-chart registry JSON, and record its SHA256.

- [ ] **Step 2: Write failing Insights tests**

  Assert the metrics-01 block marker and structural lines, exactly three supported metrics, current snapshot totals and date, the unchanged screen-reader summary immediately before the chart, visits/views series only, source caption, sampled note, no table, and no demo terminology or values.

- [ ] **Step 3: Run focused tests and verify RED**

  Run: `npx vitest run lib/visitor-insights.test.ts && node --test scripts/verify-build.test.mjs`

  Expected: renderer-layout and exported-value assertions fail against the current Insights markup.

- [ ] **Step 4: Vendor the licensed chart and adapt metrics-01**

  Keep an exact staged payload and license notice, place required chart source under the local registry namespace, and adapt import paths only where necessary for this repository. Map the committed daily points to visits and views. Remove all demo changes, session duration, unique visitor, and total session fields.

- [ ] **Step 5: Resolve declared dependencies with the prescribed lockfile command**

  Add only dependencies required by the exact chart payload, merge them with Task 1 dependency edits, and run exactly `npx npm@10.9.0 install --package-lock-only`.

- [ ] **Step 6: Verify focused tests and landing budget**

  Repeat Step 3 and run `npm run build && node scripts/perf-gate.mjs`. Require zero test failures, no initial-route budget growth beyond the gate, and Lighthouse at least 0.90.

### Task 4: experience-01 Timeline layout

**Files:**
- Create: `components/registry/experience-01.tsx`
- Modify: `components/registry/work-experience.tsx`
- Modify: `components/sections/timeline.tsx`
- Modify: `styles/globals.css`
- Modify: `docs/components-map.md`
- Modify: `lib/registry-data-adapters.test.tsx`
- Modify: `lib/core-sections.test.ts`
- Modify: `scripts/verify-build.mjs`
- Modify: `scripts/verify-build.test.mjs`

**Interfaces:**
- Consumes: `groupTimelineExperience(profile.timeline)` and the staged experience-01/work-experience/style registry items.
- Produces: experience-01 structure inside `#timeline`, preserving seven organizations and eight non-education positions in profile order, with native collapsed details controls.

- [ ] **Step 1: Write failing Timeline adoption tests**

  Require the experience-01 marker and screen-line layout, seven organizations, eight positions, profile order and text, no demo organizations or external logos, and eight keyboard-accessible native collapsed disclosures with synchronized `aria-expanded` state.

- [ ] **Step 2: Run focused tests and verify RED**

  Run: `npx vitest run lib/registry-data-adapters.test.tsx lib/core-sections.test.ts && node --test scripts/verify-build.test.mjs`

  Expected: the missing experience-01 wrapper and exported marker fail.

- [ ] **Step 3: Adapt and integrate experience-01**

  Replace hard-coded registry data with adapter props, retain the existing Fig. 7 title and anchor, keep education records in the separate Education section, use dossier line and typography tokens, and retain native details/summary controls. Do not add remote logos, Motion chevrons, or demo expanded state.

- [ ] **Step 4: Run focused tests and verify GREEN**

  Repeat Step 2 and require zero failures.

### Task 5: Full verification, review, and conventional commits

**Files:**
- Verify: all changed tracked files
- Verify generated export: `out/index.html` and `out/404.html`

**Interfaces:**
- Consumes: all four completed registry integrations.
- Produces: a reviewed `tri/31-polish6` branch with coherent conventional commits and all required evidence.

- [ ] **Step 1: Run every required gate fresh**

  Run `npx vitest run`, `npm test`, `npm run build`, `node scripts/perf-gate.mjs`, and `npm run layout:gate` as separate fresh commands. Require exit code 0 and Lighthouse at least 0.90.

- [ ] **Step 2: Verify exported HTML and static serving**

  Require `out/404.html` to contain ZST, fallback text, and the home link with no chanhdai runtime strings; require `#insights` in `out/index.html` to contain 40 visits, 60 views, 2026-08-30, and the new metrics markup; require `#timeline` to contain experience-01 markup and all profile-derived positions. Serve `out` locally and confirm a bogus document route returns the 404 page with status 404.

- [ ] **Step 3: Review repository state and provenance**

  Run `git diff --check`, inspect the full branch diff, verify staged registry SHAs, verify Bklit license notice retention, inspect landing chunks for p5 absence, and preserve all pre-existing untracked artifacts.

- [ ] **Step 4: Commit coherent conventional slices**

  Use conventional messages. The social-proof commit body records that the logo-forward block was placed in Worked with. Confirm every commit is on `tri/31-polish6` and the branch begins at `origin/main` commit `30fe325`.
